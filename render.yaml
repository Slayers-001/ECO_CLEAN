services:
  - type: web
    name: ecoclean-3d
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:all
    startCommand: npm run start:prod
    healthCheckPath: /healthz
    envVars:
      - key: NODE_VERSION
        value: "20"
